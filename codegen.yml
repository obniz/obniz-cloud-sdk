overwrite: true
schema: 'https://api_stg.obniz.com/v1/graphql'
documents: './*.graphql'
generates:
  sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
