overwrite: true
schema: 'https://api_stg.obniz.io/v1/graphql'
documents: './*.graphql'
generates:
  sdk.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-graphql-request
